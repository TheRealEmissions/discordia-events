import { ISettings } from "./interfaces/Settings.js";

export const SettingsConfig: ISettings = {
  toggles: {
    info: true,
    debug: true,
    warn: true,
    error: true,
    userError: true,
  },
};
